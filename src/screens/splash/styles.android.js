import { StyleSheet } from 'react-native';
import Color from '../../assets/colors';

export default StyleSheet.create({
    textNameApp: {
        fontSize: 50,
        color: Color.BLUE,
        fontWeight: 'bold',
    },
}) 